#include <iostream>
#include <cmath>
#include <Windows.h>
using namespace std;

int main() {
	const double pi = 3.14;
	double L;
	cout << L;
	double R = L / (2 * pi);
	double S = pi * pow(R, 2);
	cout << S;
	return 0;
}